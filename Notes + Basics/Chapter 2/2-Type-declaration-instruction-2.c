#include<stdio.h>

int main(){
    float a = 1.1;
    float b = a + 8.9; // agar float b = a + 8.9 pehle likhoge and then float a = 1.1 error aajega because code line to line execute hoga
    printf("The value of c is %f \n", b);
    return 0;
    
}
