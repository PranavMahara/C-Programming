#include<stdio.h>

int main(){
    int a = 61;
    printf("%d \n", a);

    a++; // ++ means add 1, +++ means error aajega lol
    printf("%d \n", a);

    a++; // ++ means add 1, +++ means error aajega lol
    printf("%d \n", a);

      
    

    return 0;
}