#include<stdio.h>

int main(){
    int a = 3;
    float b = 5.7;
    printf("The value of a is %d", (int)b); // Typecasting converting from one form to another, In this case we are converting from float to int
    // see b is printing 5
    return 0;
}
