# include<stdio.h>

int main(){
    int tom;    // This is variable tom is vairable
    tom = 34;   // ; is used to terminate the statement

    int _tom;

    int to_m; // No special symbol other than _ allowed

    int Tom;    // This is another variable, variable name is case insensitive
    Tom = 48;

    int error_code; // int error_code ka matlab hai error_code  ka variable banado memeory mein
    printf("Hello i am ATG \n"); // isko print kardo
    printf("The value of tom is ---> %d \n", tom);
    printf("The value of Tom is ---> %d \n", Tom);

    return 0;
}

// We must create meaningful variable so that after some time we remember about different variabes
// 'A' --> is a character
// Instructions are case-sensitive like return 0; ko aap RETURN 0; nahi likh sakte


/*
This is other way to write comment
any number of lines
*/ 