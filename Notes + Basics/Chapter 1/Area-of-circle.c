#include<stdio.h>

int main(){
    int radius;
    printf("Enter the value of radius ", radius);
    scanf("%f", &radius);
    float pi = 3.14;

    printf("Area of circle is %f", pi*radius*radius);   

    return 0;
}