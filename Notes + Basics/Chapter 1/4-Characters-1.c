#include<stdio.h>

int main(){
     // Character
    char g = '@';   // Now we can have anything @, a, b etc
    //integers are not suggested to have char syntax becuase they have their own indentity that is "int"
    
    
    // char f = '123'; error aajega

    printf("The value of character is %c \n", g);

    // printf("The value of f which is integer and is not suggested is %f", f);
    return 0;
}