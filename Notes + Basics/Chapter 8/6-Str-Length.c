#include<stdio.h>
#include <string.h>

int main(){
    char *st = "Arjun";
    int a = strlen(st); // other way directly write strlen(st) instead of a
    printf("The length of the string st is %d", a);



    // printf("The length of the string st is %d", strlen(st)); // Like this
    


   

    return 0;
}