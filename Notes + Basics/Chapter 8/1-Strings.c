#include<stdio.h>

int main(){         //   \0 is a nucll character, it is used to denote string termination characters stored in continuos memory location
                    // char str[] = {'A', 'R', 'J', 'U', 'N', '\0'}; // niche wali line and upar vali line same work karegi niche vali line hume compiler dwara comfort dena ka kam kar rahi hai

    char str[] = "Arjun"; // str is variable name it can be anything
    

    return 0;
}